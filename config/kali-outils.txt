# Outils Kali installés automatiquement dès la première ouverture de Kali.
# Un paquet par ligne. Les lignes vides et les # sont ignorés.
# Choisis pour fonctionner sans root (dans proot) et rester raisonnables en taille.
# Pour tout ajouter d'un coup : kali outils complet   (métapaquet kali-linux-headless)

# --- Réseau et découverte ---
nmap
netcat-traditional
tcpdump
dnsutils
whois
net-tools
iproute2
curl
wget

# --- Web ---
nikto
whatweb
dirb
sqlmap
wfuzz
wpscan

# --- Mots de passe (usage autorisé uniquement) ---
hydra
john
hashcat

# --- Analyse et divers ---
git
python3
python3-pip
metasploit-framework
